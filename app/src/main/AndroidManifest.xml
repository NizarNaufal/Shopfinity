<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".App"
        android:allowBackup="false"
        android:icon="@drawable/ic_shopfinity"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_shopfinity"
        android:supportsRtl="true"
        android:theme="@style/Theme.Shopfinity"
        tools:targetApi="31">
        <activity
            android:name=".splash.screen.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.screen.LoginActivity"/>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
        <activity
            android:name=".detail.screen.DetailActivity"
            android:exported="true" />
    </application>

</manifest>